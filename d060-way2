#include <iostream>

using namespace std;

int main()
{
    int tn; //tn 表示現在時間
    cin >> tn;
    cout << (85-tn) % 60;
    //如果tn介於(含)26~59分 那 用85(概念是下一次25分 故60+25=85)減去即為還需要的時間 且此值一定小於60
    //又如果 tn 介於(含)0~25 那用85減去 會得到所需時間+60分 重點 用60取餘數 必定為1...餘所需時間
    //因為第一行值小於60 所以對60取餘數不會影響 綜上 最後可得 (85-tn) % 60 即為所求.  
    
    //給未來的我 畫長條圖較好懂 標上 0 25 60 85 4點 


}
